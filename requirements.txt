# Haupt-Dependencies
opencv-python>=4.8.0
numpy>=1.21.0
stupidArtnet>=1.3.0
jsonschema>=4.0.0
flask[async]>=3.0.0
flask-cors>=4.0.0
flask-socketio>=5.3.0
python-socketio>=5.11.0
msgpack>=1.0.0
Pillow>=10.0.0
aiortc>=1.6.0
av>=10.0.0  # Video processing & audio decoding (FFmpeg bindings)
sounddevice>=0.4.0  # Audio input/output for parameter sequences & sequencer
# BPM detection: using numpy-only implementation (no external library needed)

# Output routing system
screeninfo>=0.8.0  # Multi-monitor detection
# NDIlib-python>=1.0.0  # NDI network video output (optional - install separately if needed)
# SpoutGL>=0.2.0  # Spout GPU texture sharing (optional, Windows only)

# GPU compositing pipeline (wgpu/Vulkan)
wgpu>=0.19.0         # WebGPU native — Vulkan/D3D12 backend, no OpenGL required
psutil>=5.9.0        # System RAM / CPU monitoring (performance profiler)
